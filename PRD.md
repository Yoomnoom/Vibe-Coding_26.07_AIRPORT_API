# 인천공항 혼잡도 대시보드 — PRD

- 버전: v0.3 / 작성일: 2026-07-23 / 최종 수정: 2026-07-23 / 상태: 1차 구현 완료 (목업 데이터 + Supabase Auth/DB 실연동, 공공데이터포털 API는 아직 목업)

## 목차
1. 개요
2. 핵심 기능
3. 비기능 요구사항
4. 아키텍처
5. 외부 API (공공데이터포털)
6. 데이터 모델 (Supabase)
7. 화면 구성
8. 리스크 & 미결정 사항
9. 범위 밖

---

## 1. 개요

공공데이터포털의 **인천공항공사_여객터미널혼잡도예고제** 데이터를 활용해, 사용자가 선택한 날짜·시간의 터미널별(T1/T2) 입국장·출국장 혼잡도를 숫자와 그래프로 보여주는 웹 대시보드.

- **대상:** 공항 방문객(비로그인도 조회 가능), 반복 방문 사용자(로그인 후 즐겨찾기)
- **비목표:** 실시간 초단위 트래킹, 항공편/예약 연동, 알림

---

## 2. 핵심 기능

| # | 기능 | 요구사항 | 상태 |
|---|---|---|---|
| 1 | 오늘/내일 조회 | 진입 시 "오늘" 기본 표시, 토글로 "내일" 전환 | 구현 (목업 데이터) |
| 2 | 날짜·시간 상세 보기 | 날짜+시간 선택 → 터미널×구역(입국/출국) 4개 카드로 수치·인원수 표시 | 구현 |
| 3 | 실시간 모드 + 자동 새로고침 | 기본값은 "실시간"(현재 시각 기준), 5분마다 자동 갱신. 사용자가 날짜/시간을 직접 바꾸면 수동 모드로 전환되고 "실시간으로" 버튼으로 복귀 가능 | 구현 |
| 4 | 장소·시간별 혼잡도 히트맵 | T1/T2 × 입국장/출국장 × 00~23시(1시간 단위) 매트릭스, 색상=혼잡도 단계, 90% 이상 🔥 표시, 클릭 시 해당 시간으로 이동 | 구현 |
| 5 | 터미널 비교 그래프 | 시간대별 T1 vs T2 막대+라인 콤보 차트(recharts), 입국/출국 존 토글 | 구현 |
| 6 | 추천 시간 배너 | 선택한 존 기준 T1/T2 각각 가장 여유로운(혼잡도 최저) 시간 추천, 클릭 시 해당 시간으로 이동 | 구현 |
| 7 | 다크모드 | 즉시 전환, 로컬 저장, `prefers-color-scheme` 기본값 | 구현 |
| 8 | 로그인/회원가입 | Supabase Auth(이메일/비밀번호), 헤더에서 로그인/회원가입 팝오버 | 구현 |
| 9 | 즐겨찾기 | 로그인 시에만 추가/삭제, 본인 것만 조회(RLS), T1/T2 그룹으로 접기/펼치기, 혼잡(🔥) 표시 | 구현 (Supabase 실연동) |

### 인원수 추정 (임시)

상세 카드에 "약 N명"으로 표시되는 인원수는 실제 API에 인원수 필드가 있는지 확인되지 않아, 혼잡도 100% 기준 가정 인원(`ESTIMATED_ZONE_CAPACITY = 800`, `src/services/congestionService.ts`)으로 역산한 **임시 추정치**다. 실제 공공데이터 응답에 인원수 관련 필드가 있으면 그걸로 교체한다.

---

## 3. 비기능 요구사항

- 성능: 조회 후 3초 이내 반영 목표
- 접근성: 혼잡도는 색상+숫자/텍스트 병행 표기
- 반응형: PC + 모바일 브라우저
- 브라우저: 최신 Chrome/Edge/Safari

---

## 4. 아키텍처

```
React(Vite) SPA ──fetch──> 공공데이터포털 API (프론트 직접 호출)
       │
       └─ Supabase JS Client ─ Auth / Database(RLS)
Vercel 배포, 환경변수: .env(로컬) / Vercel Env(운영)
  - VITE_AIRPORT_API_KEY
  - VITE_SUPABASE_URL
  - VITE_SUPABASE_ANON_KEY
```

- 프론트엔드: React 19 + TypeScript + Vite (백엔드 서버 없음)
- 인증/DB: Supabase (Auth + Postgres + RLS), 프로젝트: "Yoomnoom's Project" (`pkucszwwnwpzvzqczmhh`)
- 차트: recharts
- 디자인 시스템: `DESIGN.md`(Elice 브랜드 톤 — 보라 `#7353ea` 주 색상, Pretendard Variable 폰트, 그림자 없는 플랫 UI)를 프로젝트 스타일에 맞게 적용
- 배포: Vercel (예정, 아직 미배포)

---

## 5. 외부 API (공공데이터포털)

- **데이터셋:** 인천국제공항공사_승객예고-출·입국장별 (여객터미널혼잡도예고제)
- **엔드포인트:** `https://apis.data.go.kr/B551177/passgrAnncmt/getPassgrAnncmt` (반드시 HTTPS)
- **함수명(고정):** `getPassgrAnncmt(selectdate: 0 | 1)` — 데이터 호출 서비스 계층의 함수명은 이 이름을 그대로 사용한다. (`0` = 오늘, `1` = 내일)
- **인증:** 쿼리 파라미터 `serviceKey` (`.env`의 `VITE_AIRPORT_API_KEY`)
- **응답 포맷:** 공공데이터포털 공통 REST 래퍼 가정 — `response.header.resultCode/resultMsg`, `response.body.items`. `resultCode !== '00'`이면 API 레벨 오류(트래픽 제한, 점검 등)로 간주해 에러로 처리한다.

**구현 착수 전 재확인 필요** (아직 실제 API 문서 전체 미조회 — 1차 연동 코드는 위 가정으로 작성하고, 실제 콘솔 응답 확인 후 필드 타입을 확정한다):
- 응답 필드(터미널/구역/시간대/등급 체계 등) 상세 스펙, 조회 가능 기간, 시간 granularity, CORS 허용 여부, 트래픽 제한

---

## 6. 데이터 모델 (Supabase)

### `favorites` 테이블

| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | uuid PK | default gen_random_uuid() |
| user_id | uuid FK→auth.users | 소유자, on delete cascade |
| terminal | text | 'T1' \| 'T2' (CHECK 제약) |
| zone | text | '입국장' \| '출국장' (CHECK 제약) |
| adate | date | 대상 날짜 (요일 반복 여부 미결정) |
| atime | time | 대상 시간 |
| created_at | timestamptz | default now() |

### RLS 정책
- `SELECT`/`INSERT`/`DELETE` 모두 `auth.uid() = user_id` 조건만 허용
- `UPDATE` 없음 (삭제 후 재생성)

---

## 7. 화면 구성

실제 구현된 페이지 흐름은 위→아래로 다음과 같다 (단일 페이지, 별도 라우팅 없음):

1. **헤더** — 타이틀, 오늘/내일 토글, 새로고침, 다크모드, 로그인/회원가입(비로그인) 또는 이메일+로그아웃(로그인)
2. **즐겨찾기 + 추천 시간** (한 줄, 좌우 배치)
   - 즐겨찾기: T1/T2 그룹으로 접기/펼치기, 항목 클릭 시 해당 날짜/시간으로 이동, 혼잡 항목 🔥 표시
   - 추천 시간: 선택된 존 기준 T1/T2 각각 가장 여유로운 시간
3. **상세 혼잡도 카드** — 날짜/시간 선택기 내장, 실시간 모드 표시, 터미널×구역 4개 카드(수치·인원수·즐겨찾기 추가)
4. **장소·시간별 혼잡도 히트맵** (1시간 단위)
5. **터미널 비교 그래프** (막대+라인)
6. **실시간 API 연동 테스트 섹션** — `getPassgrAnncmt` 원본 응답 확인용, 기본 접힘

---

## 8. 리스크 & 미결정 사항

**리스크**
- API 키가 프론트에서 직접 호출되어 브라우저에 노출됨 (`.env`/Vercel Env는 소스 하드코딩 방지용일 뿐, 런타임 노출은 못 막음)
- 공공데이터 API가 CORS를 막을 경우 별도 프록시(Vercel Serverless Function) 필요 — 착수 전 테스트 필수
- "혼잡도예고제"는 예측 데이터라 실제와 차이 가능 → UI 문구로 안내

**결정된 사항 (구현 반영됨)**
1. 즐겨찾기는 특정 날짜 1회성으로 구현 (`adate`/`atime` 컬럼, 요일 반복 없음)
2. 그래프 라이브러리: recharts로 결정
3. 비로그인 사용자도 혼잡도 조회(오늘/내일, 상세, 히트맵, 추천, 그래프)는 전부 가능 — 즐겨찾기 추가/조회만 로그인 필요
4. 디자인 가이드: `DESIGN.md`(Elice 톤) 적용

**여전히 미결정 (구현 전 확인 필요)**
1. 혼잡도 API 실제 응답 스펙 미확인 — 신청 후 공유 필요, 현재는 목업 데이터(`src/services/congestionService.ts`)로 화면만 완성된 상태
2. CORS 테스트 결과에 따라 프록시 구조(Vercel Serverless Function) 전환 여부
3. 다국어 지원 여부
4. 실제 인원수 필드 존재 여부 (현재는 혼잡도 %에서 역산한 추정치)

---

## 9. 범위 밖

- 실시간 알림(푸시/이메일)
- 항공편 개별 조회/예약 연동
- 다국어 지원 (미결정)
- 관리자 대시보드/통계
